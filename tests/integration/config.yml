instances:
  - id: local-integration
    repository: /backup/repo
    schedule: "* * * * *" # Every minute for fast integration testing
    env:
      RESTIC_PASSWORD: testpass

# Global defaults (can be overridden by labels)
retention: "7d:4w:6m"
stopAttached: true

mesh:
  nodeName: server1
  authPassword: mesh
  peers:
    - http://marina-it-mesh-2:8081
