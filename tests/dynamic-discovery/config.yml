instances:
  - id: local-backup
    repository: /backup/restic
    schedule: "0 2 * * *" # Daily at 2 AM
    env:
      RESTIC_PASSWORD: changeme

# Global defaults for all backup targets
retention: "7d:4w:6m"
stopAttached: false
